* VC Package Installation

#+begin_src emacs-lisp
(use-package modern-tab-bar
  :vc (:url "https://github.com/aaronjensen/emacs-modern-tab-bar"
       :rev :newest))
#+end_src

* TO DO

- [ ] Treemacs and searching the whole projects
- [ ] consult-find/find-dired/consult-fd
- [ ] Org files do no collapse, or maybe collapse after a ridiculous amount of levels
- [ ] When I `C-o` after searching and hit enter on the file name it opens it in a new window instead of just using the current open buffer
- [ ] Potentially have whitespace on the left side when something is inserted for git review
- [ ] Git review when renamed it shows the whole file as green rather than just the delta
- [ ] How do you "connect" Claude to Emacs (lines highlighted etc)
- [ ] Git review allows you to comment on things and then generate a output of the comments with file names and locations to pass to Claude to fix things
- [ ] Fix magit blame not going to the commit on enter
  ```
  (with-eval-after-load 'magit-blame
    (evil-define-key 'normal magit-blame-mode-map
      (kbd "RET") #'magit-show-commit))
  ```
- [ ] Update packages
- [ ] https://github.com/mwac-dev/grease.el
