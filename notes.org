* VC Package Installation

#+begin_src emacs-lisp
(use-package modern-tab-bar
  :vc (:url "https://github.com/aaronjensen/emacs-modern-tab-bar"
       :rev :newest))
#+end_src

* TO DO

- [ ] Window numbering is not taking precedence
- [ ] Turn off the bell for a window not existing (etc)
- [ ] No line wrapping until visual-line-mode is turned on
- [ ] VTerm popup goes across the full bottom (instead of going to treemacs if open)
