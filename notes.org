* VC Package Installation

#+begin_src emacs-lisp
(use-package modern-tab-bar
  :vc (:url "https://github.com/aaronjensen/emacs-modern-tab-bar"
       :rev :newest))
#+end_src

* TO DO

- [ ] Opening a project seems to carry forward the last open buffer
- [ ] Treemacs folders don't collapse
- [ ] "jk" doesn't close Treemacs
- [ ] VTerm toggle opens across the bottom, not at the bottom of the current buffer
